<%= form_with(model: @salesReturn, local: true) do |form| %>
  <% if @salesReturn.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@salesReturn.errors.count, "error") %>
      prohibited this salesReturn from being saved:
    </h2>

    <ul>
      <% @salesReturn.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

<section class="content">
  <div class="row">
    <div class="col-md-12">
      <div class="box box-primary">
        <div class="box-header with-border"></div>
        <form role="form">
          <div class="box-body">
            <div class="col-md-6">
              <div class="form-group">
                <%= form.label :sale_id %>
                <%= form.select :sale_id, Sale.pluck(:id, :id), class: 'form-control', prompt: 'Select sale' %>
              </div>
              <div class="form-group ">
                <%= form.label :customer_id %>
                 <%= form.select :customer_id, Customer.pluck(:customer_name, :id), class: 'form-control', prompt: 'Select Customer' %>
              </div>
              <div class="form-group ">
                <%= form.label :product_id %>
                <%= form.select :product_id, Product.pluck(:product_name, :id), class: 'form-control', prompt: 'Select product' %>
              </div>
              <div class="form-group ">
                <%= form.label :salesreturn_date %>
                <%= form.date_field :salesreturn_date,class: 'form-control', placeholder:'yyyy/mm/dd'%>
                </div>
              <div class="form-group ">
                <%= form.label :unit %>
                <%= form.select :unit, Sale.pluck(:unit, :id), class: 'form-control', prompt: 'Select unit' %>
              </div>
            </div>
            <div class="col-md-6">
              <div class="form-group ">
              <%= form.label :price %>
             <%= form.select :price, Product.pluck(:product_price, :id), class: 'form-control', prompt: 'Select product' %>
              </div>
            </div>
            <div class="clearfix"></div>
            <div class='col-md-6'>
              <%= form.submit class: 'btn btn-primary'%></div>
          </div>
        </div>
      </form>
    </div>
  </div>
</section>
<% end %>
